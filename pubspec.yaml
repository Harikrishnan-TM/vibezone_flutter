name: vibezone_flutter
description: "A new Flutter project."
publish_to: 'none' # Prevent publishing to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Main packages
  http: ^0.13.5
  agora_rtc_engine: ^6.2.4
  web_socket_channel: ^2.4.0
  flutter_dotenv: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # If you want to include images or fonts later:
  # assets:
  #   - assets/
